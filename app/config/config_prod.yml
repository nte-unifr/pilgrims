imports:
    - { resource: config.yml }
    - { resource: security_prod.yml }

#framework:
#    validation:
#        cache: validator.mapping.cache.doctrine.apc
#    serializer:
#        cache: serializer.mapping.cache.apc

#doctrine:
#    orm:
#        metadata_cache_driver: apc
#        result_cache_driver: apc
#        query_cache_driver: apc

monolog:
    handlers:
        main:
            type:         fingers_crossed
            action_level: error
            handler:      nested
        nested:
            type:  stream
            path:  "%kernel.logs_dir%/%kernel.environment%.log"
            level: debug
        console:
            type:  console

parameters:
    app.path.full.site_images: /pilgrims%app.path.site_images%

vich_uploader:
    mappings:
        site_image:
            uri_prefix: /pilgrims%app.path.site_images%

liip_imagine:
  resolvers:
        default:
            web_path:
                web_root: %kernel.root_dir%/../web # %kernel.root_dir%/../web
